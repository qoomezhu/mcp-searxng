# MCP 协议升级说明 (2026-02-19)

## 📋 更新概述

本次更新将 MCP 服务器升级到最新的 **MCP 2025-11-25** 规范，并添加了多项安全增强功能。

### 提交信息
- **Commit SHA**: `38f2ad9f12398cd9c451a128734a745cd47a56f9`
- **提交时间**: 2026-02-19T11:08:11Z
- **查看提交**: https://github.com/qoomezhu/mcp-searxng/commit/38f2ad9f12398cd9c451a128734a745cd47a56f9

---

## 🔄 主要变更

### 1. MCP SDK 版本升级

| 项目 | 之前 | 现在 |
|------|------|------|
| **@modelcontextprotocol/sdk** | `1.17.4` | `^1.26.0` |
| **MCP 规范版本** | ~2025-03-26 | **2025-11-25** |

**影响**:
- ✅ 兼容最新的 MCP 客户端
- ✅ 支持新增的 Prompts 能力
- ✅ 获得最新的安全补丁和性能优化

---

### 2. 新增 Prompts 功能 🆕

**文件**: `src/prompts.ts` (新建)

提供了 4 个预定义的搜索模板：

#### 2.1 tech-news
搜索最新技术新闻
```typescript
{
  name: "tech-news",
  description: "Search for latest technology news and articles",
  arguments: [
    { name: "topic", required: true },
    { name: "time_range", required: false }
  ]
}
```

**使用示例**:
```
使用 tech-news 提示，topic="AI 大模型", time_range="month"
```

#### 2.2 academic-search
搜索学术论文
```typescript
{
  name: "academic-search",
  description: "Search for academic papers and research articles",
  arguments: [
    { name: "field", required: true },
    { name: "year", required: false }
  ]
}
```

**使用示例**:
```
使用 academic-search 提示，field="机器学习", year="2024"
```

#### 2.3 recent-updates
搜索最近更新
```typescript
{
  name: "recent-updates",
  description: "Search for recent updates on a specific topic",
  arguments: [
    { name: "topic", required: true }
  ]
}
```

#### 2.4 how-to
搜索教程指南
```typescript
{
  name: "how-to",
  description: "Search for tutorials and how-to guides",
  arguments: [
    { name: "task", required: true },
    { name: "level", required: false }
  ]
}
```

---

### 3. URL 安全验证增强 🔒

**文件**: `src/url-reader.ts` (更新)

#### 3.1 SSRF 攻击防护

新增了 `validateUrl()` 函数，阻止以下类型的 URL：

| 类型 | 示例 | 状态 |
|------|------|------|
| **非 HTTP/HTTPS** | `file://`, `ftp://`, `gopher://` | ❌ 阻止 |
| **本地地址** | `localhost`, `127.0.0.1` | ❌ 阻止 |
| **内网地址** | `192.168.x.x`, `10.x.x.x` | ❌ 阻止 |
| **链路本地** | `169.254.x.x`, `fe80::/10` | ❌ 阻止 |
| **IPv6 本地** | `::1`, `fc00::/7` | ❌ 阻止 |

**代码示例**:
```typescript
function validateUrl(urlString: string): { valid: boolean; error?: string } {
  const url = new URL(urlString);
  
  // 只允许 HTTP/HTTPS
  if (!["http:", "https:"].includes(url.protocol)) {
    return { valid: false, error: "Only HTTP and HTTPS URLs are supported" };
  }
  
  // 阻止内网地址
  const blockedPatterns = [
    /^localhost$/i,
    /^127\./,
    /^10\./,
    /^172\.(1[6-9]|2[0-9]|3[0-1])\./,
    /^192\.168\./,
    // ...
  ];
  
  for (const pattern of blockedPatterns) {
    if (pattern.test(hostname)) {
      return { valid: false, error: "Access to internal addresses is not allowed" };
    }
  }
  
  return { valid: true };
}
```

---

### 4. Dockerfile 优化 🐳

**文件**: `Dockerfile` (更新)

#### 4.1 构建优化

**之前**:
```dockerfile
COPY ./ /app
RUN --mount=type=cache,target=/root/.npm npm run bootstrap
```

**现在**:
```dockerfile
COPY package*.json ./
RUN --mount=type=cache,target=/root/.npm npm ci
COPY . .
RUN npm run build
```

**优势**:
- ✅ 更好的 Docker 层缓存
- ✅ 依赖变更时只重新安装依赖
- ✅ 代码变更时跳过依赖安装

#### 4.2 添加 dumb-init

```dockerfile
RUN apk update && apk upgrade && apk add --no-cache dumb-init
ENTRYPOINT ["dumb-init", "--", "node", "dist/index.js"]
```

**优势**:
- ✅ 正确处理信号（SIGINT, SIGTERM）
- ✅ 避免僵尸进程
- ✅ 更快的容器关闭

---

### 5. MCP 服务器能力更新

**文件**: `src/index.ts` (更新)

#### 5.1 新增 Prompts 能力

```typescript
const server = new Server(
  { name: "ihor-sokoliuk/mcp-searxng", version: packageVersion },
  {
    capabilities: {
      logging: {},
      resources: {},
      tools: { /* ... */ },
      prompts: {}, // 新增
    },
  }
);
```

#### 5.2 新增请求处理器

```typescript
// 列出所有可用的 prompts
server.setRequestHandler(ListPromptsRequestSchema, async () => {
  return { prompts: Object.values(SEARCH_PROMPTS) };
});

// 获取特定 prompt 的模板
server.setRequestHandler(GetPromptRequestSchema, async (request) => {
  const { name, arguments: args } = request.params;
  const validation = validatePromptArgs(name, args);
  if (!validation.valid) {
    throw new Error(validation.error);
  }
  const template = getPromptTemplate(name, args || {});
  return {
    messages: [{ role: "user", content: { type: "text", text: template } }]
  };
});
```

---

## 📊 功能对比

| 功能 | 之前 | 现在 |
|------|------|------|
| **MCP SDK 版本** | 1.17.4 | 1.26.0 ✅ |
| **MCP 规范** | ~2025-03-26 | 2025-11-25 ✅ |
| **Tools** | ✅ | ✅ |
| **Resources** | ✅ | ✅ |
| **Logging** | ✅ | ✅ |
| **Prompts** | ❌ | ✅ 新增 |
| **Elicitation** | ❌ | ⏳ 待实现 |
| **Roots** | ❌ | ⏳ 待实现 |
| **Sampling** | ❌ | ⏳ 待实现 |
| **URL 安全验证** | ❌ | ✅ 新增 |
| **Docker 信号处理** | ❌ | ✅ 新增 |

---

## 🚀 使用指南

### 安装依赖

```bash
npm install
```

### 构建项目

```bash
npm run build
```

### 使用 Prompts

在支持 MCP Prompts 的客户端中：

1. **列出可用 prompts**:
   ```
   列出可用的搜索模板
   ```

2. **使用 tech-news 模板**:
   ```
   使用 tech-news 模板搜索 AI 大模型的最新新闻
   ```

3. **使用 academic-search 模板**:
   ```
   使用 academic-search 模板查找 2024 年机器学习论文
   ```

### Docker 使用

```bash
# 构建镜像
docker build -t mcp-searxng:latest .

# 运行容器
docker run -e SEARXNG_URL=https://searx.example.com mcp-searxng:latest
```

---

## 🔍 测试验证

### 1. 验证 Prompts 功能

```bash
# 使用 MCP Inspector
npm run inspector

# 在 Inspector 中测试
# 1. 连接到服务器
# 2. 调用 prompts/list
# 3. 调用 prompts/get (name: "tech-news", arguments: { topic: "AI" })
```

### 2. 验证 URL 安全

```bash
# 应该成功
curl http://example.com

# 应该失败（内网地址）
curl http://192.168.1.1

# 应该失败（localhost）
curl http://localhost:8080

# 应该失败（非 HTTP 协议）
curl file:///etc/passwd
```

---

## ⚠️ 注意事项

### 破坏性变更

无破坏性变更。所有现有功能保持向后兼容。

### 已知限制

1. **Elicitation 功能**: 尚未实现（计划中）
2. **OAuth 2.1**: 仍使用 Basic Auth（计划中）
3. **速率限制**: 尚未实现（建议添加）

---

## 📝 后续计划

### 短期 (1-2 周)

- [ ] 添加 Elicitation 功能
- [ ] 实现速率限制
- [ ] 添加单元测试覆盖 Prompts

### 中期 (1-2 月)

- [ ] OAuth 2.1 + PKCE 支持
- [ ] 添加 Roots 功能
- [ ] 实现 Sampling 能力

### 长期 (3-6 月)

- [ ] Tasks 功能（异步任务）
- [ ] 工具图标元数据
- [ ] 性能监控和指标

---

## 📚 参考文档

- [MCP 官方规范 (2025-11-25)](https://modelcontextprotocol.io/specification/2025-11-25)
- [MCP SDK GitHub](https://github.com/modelcontextprotocol/typescript-sdk)
- [SSRF 防护最佳实践](https://owasp.org/www-community/attacks/Server_Side_Request_Forgery)

---

## 🤝 贡献者

- **更新执行**: AI Assistant
- **仓库维护**: qoomezhu
- **原始作者**: Ihor Sokoliuk (@ihor-sokoliuk)

---

**更新日期**: 2026-02-19  
**文档版本**: 1.0
