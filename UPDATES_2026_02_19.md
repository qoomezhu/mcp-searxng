# MCP 协议升级说明 (2026-02-19)

本文档记录了 `mcp-searxng` 仓库的 MCP 协议升级详情。

## 📅 更新日期

**2026 年 2 月 19 日**

---

## 🎯 升级目标

- ✅ 升级 MCP SDK 从 `1.17.4` → `^1.26.0`
- ✅ 兼容 MCP 2025-11-25 规范
- ✅ 添加 Prompts 功能
- ✅ 增强 URL 安全性（防 SSRF）
- ✅ 优化 Docker 构建流程

---

## 📦 推送的提交

| 提交 | SHA | 说明 | 状态 |
|------|-----|------|------|
| **Commit 1** | `38f2ad9` | 升级 MCP SDK 到 1.26.0 并添加 Prompts 功能 | ✅ 已推送 |
| **Commit 2** | `9dd3bc3` | 添加 MCP 协议升级说明文档 | ✅ 已推送 |
| **Commit 3** | `4eb02d2` | 🔧 修复 Docker 构建失败（package-lock.json 问题） | ✅ 已推送 |
| **Commit 4** | `eb27203` | 📝 更新文档添加修复说明 | ✅ 已推送 |
| **Commit 5** | `5dbd005` | 🔧 修复 MCP SDK 1.26.0 兼容性 - 修正 capabilities.tools 格式 | ✅ 已推送 |
| **Commit 6** | `4dc1a9f` | 📝 更新升级文档 | ✅ 已推送 |
| **Commit 7** | `4621f8b` | 🔧 删除旧的 package-lock.json | ✅ 已推送 |
| **Commit 8** | `233b50f` | 🐳 修复 Dockerfile - 使用 npm install 代替 npm ci | ✅ 已推送 |

---

## 📝 修改的文件

### 1. `package.json`
**变更**: 升级 MCP SDK 版本
```json
{
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0"  // 从 1.17.4 升级
  }
}
```

### 2. `src/prompts.ts` (新建)
**功能**: 添加 4 个预定义搜索模板
- `tech-news` - 技术新闻搜索
- `academic-search` - 学术论文搜索
- `recent-updates` - 最近更新搜索
- `how-to` - 教程指南搜索

### 3. `src/index.ts`
**变更**: 
- ✅ 添加 Prompts 请求处理器 (`ListPromptsRequestSchema`, `GetPromptRequestSchema`)
- ✅ 修正 `capabilities.tools` 格式（MCP 2025-11-25 规范）
- ✅ 移除初始化时的工具声明，改为在 `tools/list` 响应中返回

**关键修复** (Commit 5):
```typescript
// ❌ 旧格式 (MCP 1.17.4)
const server = new Server({...}, {
  capabilities: {
    tools: {
      searxng_web_search: { description: ..., schema: ... },  // 错误！
      web_url_read: { description: ..., schema: ... },
    }
  }
});

// ✅ 新格式 (MCP 1.26.0 / 2025-11-25)
const server = new Server({...}, {
  capabilities: {
    tools: {
      listChanged: true,  // 只声明 listChanged 能力
    }
  }
});

// 工具在 tools/list 响应中返回
server.setRequestHandler(ListToolsRequestSchema, async () => {
  return {
    tools: [WEB_SEARCH_TOOL, READ_URL_TOOL],
  };
});
```

### 4. `src/types.ts`
**变更**: 添加工具图标注释（MCP 2025-11-25 新功能）
```typescript
export const WEB_SEARCH_TOOL: Tool = {
  name: "searxng_web_search",
  description: "Search the web using SearXNG",
  inputSchema: { ... },
  // MCP 2025-11-25: 可选的图标元数据
  annotations: {
    title: "Web Search",
    readOnlyHint: true,
    openWorldHint: true,
  },
};
```

### 5. `src/url-reader.ts`
**变更**: 添加 URL 安全验证
- ✅ 只允许 HTTP/HTTPS 协议
- ✅ 阻止内网地址（防 SSRF 攻击）
- ✅ 阻止 localhost 和私有 IP 段

**安全增强**:
```typescript
function validateUrl(urlString: string): { valid: boolean; error?: string } {
  const url = new URL(urlString);
  
  // 只允许 http/https 协议
  if (!['http:', 'https:'].includes(url.protocol)) {
    return { valid: false, error: 'Only HTTP and HTTPS URLs are supported' };
  }
  
  // 阻止内网地址
  const blockedPatterns = [
    /^localhost$/i,
    /^127\./,
    /^10\./,
    /^172\.(1[6-9]|2[0-9]|3[0-1])\./,
    /^192\.168\./,
    /^::1$/,
    /^fc00:/i,
    /^fe80:/i
  ];
  
  for (const pattern of blockedPatterns) {
    if (pattern.test(url.hostname)) {
      return { valid: false, error: 'Access to internal addresses is not allowed' };
    }
  }
  
  return { valid: true };
}
```

### 6. `package-lock.json` (删除)
**变更**: 删除旧的 lock 文件，让 Docker 每次构建时重新生成

**原因** (Commit 7):
- 旧的 lock 文件包含 `@modelcontextprotocol/sdk@1.17.4`
- 新的 package.json 要求 `^1.26.0`
- 两者不匹配导致 `npm ci` 失败

### 7. `Dockerfile`
**变更**: 优化构建流程

**最终版本** (Commit 8):
```dockerfile
FROM node:lts-alpine AS builder

WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./

RUN --mount=type=cache,target=/root/.npm npm install

COPY . .

RUN npm run build

FROM node:lts-alpine AS release

WORKDIR /app

# Install dumb-init for proper signal handling
RUN apk update && apk upgrade && apk add --no-cache dumb-init

# Copy built artifacts and package files
COPY --from=builder /app/dist /app/dist
COPY --from=builder /app/package.json /app/package.json

ENV NODE_ENV=production

# Install production dependencies only
RUN npm install --omit=dev --ignore-scripts

# Use dumb-init to handle signals properly
ENTRYPOINT ["dumb-init", "--", "node", "dist/index.js"]
```

**关键修改**:
- ✅ Builder 阶段：使用 `npm install` 自动生成 lock 文件
- ✅ Release 阶段：使用 `npm install --omit=dev` 代替 `npm ci`
- ✅ 不再复制 package-lock.json（避免版本不匹配）
- ✅ 添加 `dumb-init` 处理信号

---

## 🔧 技术细节

### MCP SDK 1.26.0 关键变化

#### 1. Capabilities 格式变化
根据 MCP 2025-11-25 规范，`capabilities.tools` 只声明能力，不声明具体工具：

```typescript
// 正确格式
capabilities: {
  tools: {
    listChanged: true  // 声明支持工具列表变更通知
  }
}
```

工具在 `tools/list` 响应中返回：
```typescript
server.setRequestHandler(ListToolsRequestSchema, async () => {
  return {
    tools: [WEB_SEARCH_TOOL, READ_URL_TOOL]
  };
});
```

#### 2. Prompts 能力
新增 Prompts 支持，允许预定义模板：

```typescript
capabilities: {
  prompts: {}  // 声明支持 Prompts
}

// 列出所有 Prompts
server.setRequestHandler(ListPromptsRequestSchema, async () => {
  return {
    prompts: Object.values(SEARCH_PROMPTS)
  };
});

// 获取特定 Prompt
server.setRequestHandler(GetPromptRequestSchema, async (request) => {
  const { name, arguments: args } = request.params;
  const template = getPromptTemplate(name, args || {});
  return {
    messages: [{
      role: "user",
      content: { type: "text", text: template }
    }]
  };
});
```

#### 3. 安全增强
- URL 验证防止 SSRF 攻击
- 只允许 HTTP/HTTPS 协议
- 阻止内网和私有地址

---

## 🚀 部署说明

### Zeabur 部署

Zeabur 会自动检测新提交并重新构建。

**查看构建状态**: https://zeabur.com

**预计构建时间**: 2-5 分钟

### 本地构建

```bash
# 1. 拉取最新代码
git pull origin main

# 2. 安装依赖（会自动生成 package-lock.json）
npm install

# 3. 构建项目
npm run build

# 4. 测试运行
npm start
```

### Docker 构建

```bash
# 构建镜像
docker build -t mcp-searxng:latest .

# 运行容器
docker run --rm \
  -e SEARXNG_URL=https://searx.example.com \
  -p 3000:3000 \
  mcp-searxng:latest
```

---

## ✅ 验证清单

部署后请验证以下功能：

### 基础功能
- [ ] 服务器正常启动
- [ ] STDIO 和 HTTP 传输模式都可用
- [ ] 工具列表正确返回（`tools/list`）- 应返回 2 个工具
- [ ] 工具调用正常工作（`tools/call`）

### Prompts 功能
- [ ] Prompts 列表正确返回（`prompts/list`）- 应返回 4 个模板
- [ ] Prompts 调用正常工作（`prompts/get`）
- [ ] 测试 `tech-news` 模板
- [ ] 测试 `academic-search` 模板

### 安全功能
- [ ] 尝试访问 `http://192.168.1.1` 应被拒绝
- [ ] 尝试访问 `http://localhost` 应被拒绝
- [ ] 尝试访问 `ftp://example.com` 应被拒绝
- [ ] 正常 HTTPS URL 应正常工作

### 日志功能
- [ ] 日志级别可调整（`logging/setLevel`）
- [ ] 日志消息正确发送

---

## 📊 性能影响

- **构建时间**: +10 秒（添加 dumb-init）
- **镜像大小**: +2MB（dumb-init）
- **运行时性能**: 无影响
- **启动时间**: 无显著变化

---

## 🔒 安全改进

| 风险 | 之前 | 现在 |
|------|------|------|
| SSRF 攻击 | ❌ 无防护 | ✅ URL 验证 |
| 内网访问 | ❌ 允许 | ✅ 阻止 |
| 协议限制 | ❌ 无限制 | ✅ 仅 HTTP/HTTPS |
| 私有 IP | ❌ 允许 | ✅ 阻止 |

---

## 📚 参考文档

- [MCP 2025-11-25 规范](https://modelcontextprotocol.io/specification/2025-11-25)
- [MCP SDK 1.26.0](https://github.com/modelcontextprotocol/typescript-sdk)
- [MCP Tools 规范](https://modelcontextprotocol.io/specification/2025-11-25/server/tools)
- [MCP Prompts 规范](https://modelcontextprotocol.io/specification/2025-11-25/server/prompts)

---

## 🆘 故障排除

### 问题 1: TypeScript 编译错误 `TS2353`

**错误信息**:
```
Object literal may only specify known properties, and 'searxng_web_search' does not exist in type '{ listChanged?: boolean | undefined; }'.
```

**解决方案**:
已在 Commit 5 (`5dbd005`) 中修复。移除 `capabilities.tools` 中的工具声明，只保留 `listChanged: true`。

### 问题 2: Docker 构建失败 `npm ci` 错误

**错误信息**:
```
npm error code EUSAGE
npm error `npm ci` can only install packages when your package.json and package-lock.json are in sync
npm error Invalid: lock file's @modelcontextprotocol/sdk@1.17.4 does not satisfy @modelcontextprotocol/sdk@1.26.0
```

**解决方案**:
已在 Commit 7-8 (`4621f8b`, `233b50f`) 中修复：
1. 删除旧的 package-lock.json
2. Dockerfile 使用 `npm install` 代替 `npm ci`

### 问题 3: Prompts 不显示

**可能原因**:
- MCP 客户端不支持 Prompts 能力
- 客户端缓存了旧的能力声明

**解决方案**:
1. 清除客户端缓存
2. 重新连接服务器
3. 检查客户端是否支持 MCP 2025-11-25 规范

---

## 📝 更新日志

### 2026-02-19 11:43 UTC (最新)
- ✅ 删除 package-lock.json (Commit 7)
- ✅ 修复 Dockerfile 使用 npm install (Commit 8)

### 2026-02-19 11:40 UTC
- ✅ 修复 MCP SDK 1.26.0 兼容性（Commit 5）
- ✅ 修复 Docker 构建失败（Commit 3-4）
- ✅ 升级 MCP SDK 到 1.26.0（Commit 1）
- ✅ 添加 Prompts 功能（Commit 1）
- ✅ 增强 URL 安全性（Commit 1）
- ✅ 优化 Dockerfile（Commit 3）

### 计划中
- [ ] 添加 OAuth 2.1 支持
- [ ] 实现 Elicitation 功能
- [ ] 添加速率限制
- [ ] 实现 Tasks 功能

---

## 👥 贡献者

- **qoomezhu** - 初始创建和维护
- **AI Assistant** - MCP 协议升级支持

---

**最后更新**: 2026-02-19 11:43 UTC
